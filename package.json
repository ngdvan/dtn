{
  "name": "seee-activity-hub",
  "version": "1.5.6",
  "private": true,
  "description": "Internal activity and task tracking for SEEE",
  "main": "app.js",
  "scripts": {
    "dev": "node --watch app.js",
    "start": "node app.js",
    "migrate": "node scripts/migrate.js",
    "check": "node --check app.js && node --check src/server.js && node --check public/app.js && node --check scripts/migrate.js"
  },
  "engines": {
    "node": ">=22 <23"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "dotenv": "^16.6.1",
    "exceljs": "^4.4.0",
    "express": "^5.1.0",
    "express-mysql-session": "^3.0.3",
    "express-session": "^1.18.2",
    "helmet": "^8.1.0",
    "multer": "^2.2.0",
    "mysql2": "^3.14.3"
  }
}
